type Collections @model @auth(rules: [{allow: public}]) {
  id: ID!
  collection: AWSJSON
}

type Users @model @auth(rules: [{allow: public}]) {
  id: ID!
  Password: String
  UserCollections: Collections @connection
}
